# svhub-frontend

Vite + React JS storefront for SV Hub.

## Setup

```bash
npm install
cp .env.example .env
npm run dev
```

The app runs at http://localhost:5173 and proxies `/api` to the Express backend on port 5000.
